//! Integration test harness for segwire.
//!
//! Provides a test harness that starts the daemon in simulation mode on a
//! D-Bus session bus, then allows running CLI operations against it.

pub mod harness;
