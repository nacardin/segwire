[package]
name = "segwire-ns-enter"
version = "0.1.0"
license = "GPL-3.0-only"
edition = "2021"
description = "Setuid helper binary for entering segwire network namespaces"

[dependencies]
nix = { workspace = true }
libc = "0.2"
